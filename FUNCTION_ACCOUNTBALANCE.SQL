SET SERVEROUTPUT ON;
CREATE OR REPLACE FUNCTION get_balance
(
   accno NUMBER
)
RETURN NUMBER
IS
   bal NUMBER;
BEGIN

SELECT BALANCE
INTO bal
FROM ACCOUNT
WHERE ACNO = accno;

RETURN bal;

EXCEPTION

WHEN NO_DATA_FOUND THEN
   RETURN 0;

END;
/