SET SERVEROUTPUT ON;
CREATE OR REPLACE TRIGGER update_trigger
BEFORE UPDATE
ON EMP
FOR EACH ROW

BEGIN

INSERT INTO NEWEMP
VALUES
(
:OLD.EID,
:OLD.EName,
:OLD.Deptno,
:OLD.Deptname,
:OLD.Gender,
:OLD.Age,
:OLD.BasicSal,
'UPDATE',
SYSDATE
);

END;
/